export const styles = theme => ({
  table: {
    borderCollapse: 'separate',
    borderSpacing: '0 3px',
    '& td:first-child': {
      fontWeight: 'bold',
      maxWidth: 340,
    },
  },
  tableBodyRow: {
    boxShadow: '0px 4px 24px rgba(0, 0, 0, 0.05)',
  },
});
