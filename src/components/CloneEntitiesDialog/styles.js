export const styles = (theme) => ({
  dilog: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
  },
  list: {
    width: "100%",
    height: 400,
    maxWidth: 300,
    backgroundColor: theme.palette.background.paper,
  },
});
