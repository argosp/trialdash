export const styles = theme => ({
  fieldTypeWrapper: {
    padding: 30,
    borderBottom: `1px solid ${theme.palette.white.dark}`,
    fontWeight: 'bold',
  },
  fieldTypeTitle: {
    marginLeft: 25,
  },
});
